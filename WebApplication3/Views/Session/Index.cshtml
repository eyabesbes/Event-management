@model IEnumerable<WebApplication3.Models.Session>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdSpeaker)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sujet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.horaire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdEvent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdSpeaker)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sujet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.horaire)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdEvent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSession }) |
            @Html.ActionLink("Details", "Detail", new { id=item.IdSession }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSession })
        </td>
    </tr>
}

</table>
